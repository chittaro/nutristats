import axios from 'axios';

/**
 * @description sends dining hall dropdown results
 * @param {object} selections list of dining hall selections
 * @returns {Promise} axios request promise
 */
export function sendHalls(selections) {
    const form = new FormData()
}